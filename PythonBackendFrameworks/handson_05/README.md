# Hands-On 5: Flask Course Manager API with SQLAlchemy ORM

This project implements a RESTful Course Management API built using **Flask**, **Flask-SQLAlchemy**, and **Flask-Migrate**. It demonstrates database modeling, relationships, blueprints, and CRUD operations using an Object-Relational Mapper (ORM).

---

